---
title: "book"
weight: 10
---

# book

Required:

- `author` (`text`) - primary author of the book.
- `published_year` (`integer`) - publication year of the edition used.

Optional:

- `original_title` (`text`) - title in the original language.
- `co_authors` (`text` or `long_text`) - additional authors/co-authors.
- `language` (`text`) - language of the specific edition/content.
- `original_language` (`text`) - original language of the work.
- `publisher` (`text`) - publishing organization.
- `publication_place` (`text`) - city/place of publication.
- `edition` (`text`) - edition label or number.
- `isbn` (`text`) - ISBN identifier.
- `pages_total` (`integer`) - total number of pages.
- `translator` (`text`) - translator name(s).
- `volume` (`text` or `integer`) - volume number/label for multi-volume books.
- `series` (`text`) - book series title.
- `reference_scope` (`text`) - scope note for `book/reference` entries.
