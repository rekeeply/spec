---
title: "Raw Model"
weight: 11
---

# Raw Model

## Contract

- Core implementations MUST include `Raw` as an account-scoped storage domain for unstructured capture.
- `Raw` MUST support quick save without selecting an active library.
- `Raw` items MUST be eligible for later processing into `Library`.

## Scope Note

- This section is intentionally minimal and will be expanded in the next iteration.
